sourcetree
	- 계정: bhj1505@kico.co.kr/정병호12!@
	TEST

	- reset정리:	
		- reset hard인 경우 로컬저장소 파일 또한 원격 해당 커밋 시점으로 바뀐다.
		- reset mixed인 경우 원격저장소 파일은 해당 시점으로 변경이 되지만 로컬저장소의 파일은 변경이 되지 않는다. 커밋 대상 건으로 처리 됨

	- reverse 정리: 
		- 커밋한 버전에서 revert 명령을 실행하면 바로 이전에 커밋한 버전을 최신 커밋시점으로 변경해줌
		- reset과의 차이점은 reset은 버전이 삭제가 되지만 reverse 같은 경우 이전 이력까지 보존
		- 주의해야 할 점은 바로 다이렉트로 이전의 버전으로 revert가 되는 것이 아니라 역순으로 단계 단계로 revert 명령을 실행하여야 한다.
		- 버전 5번 revert시 revert5번(4번의상태값), 버전 4번 revert시 revert4번(4번상태값), 버전 3번 revert 시 revert3번(2번의상태값)으로 진행
